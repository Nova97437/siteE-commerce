{% extends 'base.html.twig' %}

{% block title %}Hello ClientsController!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="multi-card row row-cols-1 row-cols-md-3 g-4 carte mt-2">
		{% for produit in produits  %}
			<div class="productCard card container mt-5" style="width: 19rem;">
				<img src="{{ produit.img }}" class="card-img-top mt-2" alt="product_picture">

				<div class="card-body">
					<h5 class="card-title">{{ produit.nom }}</h5>
					<p class="card-text">description :
						{{ produit.description }}</p>
					<p class="card-text">{{ produit.prix }}€</p>
				</div>
                <div>
                    <button><a href="{{ path('app_produits_show', {'id': produit.id}) }}">show</a></button>
                </div>
			</div>
		{% endfor %}
	</div>
{% endblock %}
